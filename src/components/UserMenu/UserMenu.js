import React from 'react';

const UserMenu = () => {
  return (
    <div>
      <p>UserMenu</p>

      <p>Welcome, Username</p>
      <button
        type="button"
        //   onClick={() => dispatch(logOut())}
      >
        Logout
      </button>
    </div>
  );
};

export default UserMenu;
